<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/eventImageLayoutRoot"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_gravity="center"
    android:visibility="visible" >

    <LinearLayout
        android:id="@+id/eventImageLayout"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:background="@drawable/image_border_0"
        android:visibility="visible" >

        <ImageView
            android:id="@+id/eventImage"
            android:layout_width="50dp"
            android:layout_height="wrap_content"
            android:adjustViewBounds="true"
            android:scaleType="fitCenter"
            android:src="@drawable/ic_weather_chance_of_rain" />
    </LinearLayout>

    <ImageView
        android:id="@+id/eventImageSmall"
        android:layout_width="25sp"
        android:layout_height="wrap_content"        
        android:layout_gravity="center"
        android:paddingRight="2sp"
        android:adjustViewBounds="true"
                
        android:src="@drawable/yrasp"
        android:visibility="gone" />
    
    <ImageView
        android:id="@+id/eventImageNotify"
        android:layout_width="12sp"
        android:layout_height="12sp"
        android:layout_gravity="center"
        android:layout_marginRight="3sp"
        android:adjustViewBounds="true"
                
        android:src="@android:drawable/ic_popup_reminder"
        android:visibility="gone" />

</LinearLayout>